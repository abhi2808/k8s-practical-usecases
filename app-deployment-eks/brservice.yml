apiVersion: v1
kind: Service
metadata:
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
  name: brservice
  labels:
    app: brservice
spec:
  type: LoadBalancer
  selector:
    app: basic-react 
  ports:
    - port: 80
      targetPort: 80
