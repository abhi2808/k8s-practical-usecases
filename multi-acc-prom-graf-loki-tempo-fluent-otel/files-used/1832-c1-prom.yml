prometheus:
  prometheusSpec:
    externalLabels:
      awsAccount: "account-1"
      clusterName: "cluster-1"
    remoteWrite:
      - url: "http://13.201.56.208:9090/api/v1/write"
    retention: 2h

alertmanager:
  enabled: false

grafana:
  enabled: false

nodeExporter:
  enabled: true

kubeStateMetrics:
  enabled: true
